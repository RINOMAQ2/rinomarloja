# Rinomaq Entregas

Projeto pronto para publicar na Vercel.

## Acessos

- Admin: `admin` / `123456`
- Breno: `breno` / `919293`
- Kleifor: `kleifor` / `919293`

## Como publicar

1. Envie todos estes arquivos para o GitHub.
2. Na Vercel, clique em **Add New > Project**.
3. Importe o repositório.
4. Framework: **Vite**.
5. Install Command: `npm install --legacy-peer-deps`.
6. Build Command: `npm run build`.
7. Output Directory: `dist`.
8. Clique em **Deploy**.

## Observação

Esta versão salva os dados no navegador por `localStorage`. Para dados compartilhados entre vários celulares ao mesmo tempo, conecte Supabase/Postgres usando o arquivo `supabase_schema.sql`.
